server:
  port: 80881

spring:
  cloud:
    config:
      git:
        uri: https://github.com/static-x03/microserviceApp
        searchPaths: confg-data
        username: ${GIT_USER}
        password: ${GIT_PASS}
  security:
    user:
      name: root
      password: s3cr3t
